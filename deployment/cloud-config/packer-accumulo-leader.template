#cloud-config

runcmd:
 - [ sh, -c, "echo \"$(curl -s http://169.254.169.254/latest/meta-data/local-ipv4) zookeeper.service.{{ cluster_domain }}\" >> /etc/hosts" ]
 - [ sh, -c, "echo \"$(curl -s http://169.254.169.254/latest/meta-data/local-ipv4) mesos-leader.service.{{ cluster_domain }}\" >> /etc/hosts" ]
 - [ sh, -c, "echo \"$(curl -s http://169.254.169.254/latest/meta-data/local-ipv4) namenode.service.{{ cluster_domain }}\" >> /etc/hosts" ]
 - [ sh, -c, "echo \"$(curl -s http://169.254.169.254/latest/meta-data/local-ipv4) accumulo-leader.service.{{ cluster_domain }}\" >> /etc/hosts" ]
 - [ sh, -c, "echo \"$(curl -s http://169.254.169.254/latest/meta-data/local-ipv4) monitoring.service.{{ cluster_domain }}\" >> /etc/hosts" ]
